// shifiting elemment in a circular queue is slow O(n) a circular queue fixes this without usong a linked list. it uses a fixes-size array and two intefer pointers head and tail when the tail reacees the head end of the array the tail wraps around index o

// its use in streaming data spu schedulling and audio buffers where memory size is fixed

class CircularQueue{
    capacity: number;
    queueArray: number[]= []
    head:
    tail:
    currentSize: number
    constructor()
}
